<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a class=" bg-red-500 rounded-md px-3 inline-block mb-2 py-2 text-sm " href="/logout"> Logout</a>
        </div>
        <h5 class="mb-5">edit your post.</h5>
        <form action="/update/<%= post._id %> " method="post">
            <textarea placeholder="Whats's on your mind?" class="block /3 p-3 outline-none resize-none bg-transparent border-2 border-zinc-500 rounded-md" name="content"><%= post.content %></textarea>
            <input class="w-42 px-3  text-sm py-2 bg-yellow-500 block rounded-md mt-2 text-black" type="submit" value="Update post">
           
        </form>
       
    </div>
    
</body>
</html>